name: Build Dybass Estudioso

# Gera os instaladores automaticamente.
on:
  push:
    branches: ['**']
    tags: ['v*']
  workflow_dispatch:   # permite rodar manualmente pelo botão "Run workflow"

jobs:
  build-windows:
    name: Windows (.exe)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Instalar dependências
        run: npm ci || npm install

      - name: Gerar instalador Windows
        run: npm run build:win

      - name: Publicar artefatos (.exe)
        uses: actions/upload-artifact@v4
        with:
          name: dybass-estudioso-windows
          path: |
            dist/*.exe
          if-no-files-found: error

  build-linux:
    name: Linux (AppImage)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Instalar dependências
        run: npm ci || npm install

      - name: Gerar AppImage
        run: npm run build:linux

      - name: Publicar artefatos (AppImage)
        uses: actions/upload-artifact@v4
        with:
          name: dybass-estudioso-linux
          path: |
            dist/*.AppImage
          if-no-files-found: error

  # Em tags (ex.: v1.0.0) cria um Release com os instaladores anexados.
  release:
    name: Publicar Release
    needs: [build-windows, build-linux]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Baixar artefatos
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Criar Release com os instaladores
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/**/*.exe
            artifacts/**/*.AppImage
          generate_release_notes: true
